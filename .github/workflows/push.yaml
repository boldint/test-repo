name: Push Workflow

on:
  push:
    branches:
      - master

jobs:
  push:
    name: Push
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2

      - name: NoOP step
        run: echo "NoOP!"
