name: Push1 Workflow

on:
  push:
    branches:
      - master
    paths-ignore:
      - '.github/**'

jobs:
  push1:
    name: Push1
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2

      - name: NoOP step
        run: echo "NoOP!"
